<?php
/**
 * Copyright © Magento, Inc. All rights reserved.
 * See COPYING.txt for license details.
 */

/** @var \Magento\Framework\View\Element\Template $block */

$objectManager = \Magento\Framework\App\ObjectManager::getInstance();
$storeManager = $objectManager->get(\Magento\Store\Model\StoreManagerInterface::class); // Отримайте екземпляр Store Manager
$currentStore = $storeManager->getStore()->getStoreId();
$message = 'You added %1 to your <a href=\"%2\">shopping cart</a>.';
if ($currentStore == 2){
    $message = 'You added %1 to your <a href=\"%2\">cart</a>.';
}
?>

<?= $block->escapeHtml(__(
    $message,
    $block->getData('product_name'),
    $block->getData('cart_url')
), ['a']);
